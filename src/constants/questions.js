const askName = `Hi! What's your name?`
const askBirthDate = `When is your birthday? (YYYY-MM-DD)`
const askNextBirthDay = `Do you want to know how many days until your next birthday?`

module.exports = { askName, askBirthDate, askNextBirthDay }